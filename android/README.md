# Android publisher app — placeholder

See [PLAN.md](../PLAN.md). Phase A wires the native build pipeline (cargo-ndk →
`jniLibs/`, uniffi-bindgen → `java/uniffi/`); this directory currently has no
committed code beyond the plan.

Layout and toolchain: see PLAN.md "Module map".
